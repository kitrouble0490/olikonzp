# Чек-лист деплоя на Timeweb

## ✅ Подготовка на локальной машине

- [ ] Установлены все зависимости (`npm install`, `composer install`)
- [ ] Выполнена сборка фронтенда (`npm run build`)
- [ ] Проверено, что приложение работает локально
- [ ] Создан `.env.example` с актуальными настройками
- [ ] Проверен `.gitignore` (исключены ненужные файлы)
- [ ] Создан архив для загрузки (опционально, через `deploy.sh`)

## ✅ Настройка на сервере Timeweb

### База данных
- [ ] Создана база данных MySQL в панели Timeweb
- [ ] Записаны данные подключения: хост, имя БД, пользователь, пароль
- [ ] Проверено подключение к БД

### Файлы
- [ ] Загружены все файлы проекта на сервер
- [ ] Файлы размещены в правильной директории (`public_html` или указанной)
- [ ] Создан файл `.env` на сервере
- [ ] Настроен `.env` файл с правильными данными:
  - [ ] `APP_ENV=production`
  - [ ] `APP_DEBUG=false`
  - [ ] `APP_URL=https://your-domain.com`
  - [ ] Данные подключения к БД
  - [ ] `APP_KEY` (будет сгенерирован)

### Зависимости
- [ ] Установлены PHP зависимости: `composer install --no-dev --optimize-autoloader`
- [ ] Установлены Node.js зависимости: `npm install` (если нужна пересборка)
- [ ] Выполнена сборка фронтенда: `npm run build`

### Laravel команды
- [ ] Сгенерирован ключ приложения: `php artisan key:generate`
- [ ] Выполнены миграции: `php artisan migrate --force`
- [ ] Запущен сидер: `php artisan db:seed --force`
- [ ] Очищен кеш: `php artisan config:clear`, `php artisan route:clear`, `php artisan view:clear`
- [ ] Создан кеш для production: `php artisan config:cache`, `php artisan route:cache`, `php artisan view:cache`

### Права доступа
- [ ] Настроены права на `storage/`: `chmod -R 775 storage`
- [ ] Настроены права на `bootstrap/cache/`: `chmod -R 775 bootstrap/cache`
- [ ] Проверен владелец файлов (должен быть пользователь веб-сервера)

### Конфигурация веб-сервера
- [ ] Настроен `.htaccess` в корне (если нужно)
- [ ] Проверен путь к `public/index.php`
- [ ] Настроены виртуальные хосты (если нужно)

## ✅ Проверка работы

- [ ] Сайт открывается в браузере
- [ ] Отображается страница авторизации
- [ ] Можно войти с учетными данными: `olikon` / `11592309`
- [ ] Загружаются данные (страницы, периоды, отделы)
- [ ] Работают API запросы (проверить в консоли браузера F12)
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в логах Laravel (`storage/logs/laravel.log`)
- [ ] CSS и JS файлы загружаются корректно
- [ ] Работает сохранение данных (проверить создание/редактирование)

## ✅ Безопасность

- [ ] `APP_DEBUG=false` в production
- [ ] `.env` файл не доступен из браузера
- [ ] Права доступа настроены правильно
- [ ] Используется HTTPS (рекомендуется)
- [ ] Пароли в БД хешируются (проверено через сидер)

## ✅ Оптимизация

- [ ] Включен кеш конфигурации
- [ ] Включен кеш маршрутов
- [ ] Включен кеш представлений
- [ ] Оптимизирован автозагрузчик Composer
- [ ] Минифицированы JS/CSS файлы (через `npm run build`)

## 📝 Полезные команды для отладки

```bash
# Просмотр логов
tail -f storage/logs/laravel.log

# Проверка конфигурации
php artisan config:show

# Проверка маршрутов
php artisan route:list

# Очистка всех кешей
php artisan optimize:clear

# Пересоздание кешей
php artisan optimize
```

## 🔄 Обновление проекта

При обновлении кода:

- [ ] Получены обновления (git pull или загрузка новых файлов)
- [ ] Установлены новые зависимости (composer, npm)
- [ ] Выполнена пересборка фронтенда (npm run build)
- [ ] Выполнены новые миграции (если есть)
- [ ] Очищен и пересоздан кеш
- [ ] Протестирована работа приложения

