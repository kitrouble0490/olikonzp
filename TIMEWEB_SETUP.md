# Настройка Laravel на Timeweb - Решение проблемы 500/403

## 🚀 Быстрое решение

**Проблема:** 500 или 403 ошибка при доступе к сайту.

**Решение:**

1. **Загрузите Laravel проект** в `~/olikonzp/` (на уровень выше `public_html/`)
2. **Используйте скрипт автоматической настройки:**
   ```bash
   # На сервере через SSH
   cd ~/olikonzp
   ./setup_timeweb.sh
   ```
3. **Или вручную:** Используйте файл `public_html_index.php` - скопируйте его в `~/public_html/index.php`

---

## Проблема

- **500 ошибка** - когда переносите файлы из `public/` в `public_html/` (неправильные пути)
- **403 ошибка** - когда не переносите файлы (Laravel файлы вне доступной директории)

## Причина

Laravel требует правильную структуру директорий. На Timeweb корневая директория - `public_html/`, но Laravel файлы должны быть на уровень выше для безопасности. Пути в `index.php` должны указывать на правильную директорию проекта.

## Решение: Два варианта

### ✅ Вариант 1: Laravel на уровень выше (БЕЗОПАСНО, РЕКОМЕНДУЕТСЯ)

**Структура:**
```
/home/username/
├── olikonzp/              ← Laravel проект
│   ├── app/
│   ├── bootstrap/
│   ├── config/
│   ├── database/
│   ├── resources/
│   ├── routes/
│   ├── storage/
│   ├── vendor/
│   ├── .env
│   └── public/            ← Исходная папка public
│       ├── .htaccess
│       ├── index.php
│       └── build/
└── public_html/           ← Корневая директория сайта
    ├── .htaccess          ← Копия из public/.htaccess
    ├── index.php          ← Используйте public_html_index.php
    └── build/             ← Копия из public/build/
```

**Шаги:**

1. **Загрузите Laravel проект** в `~/olikonzp/` (на уровень выше `public_html/`)

2. **Создайте `public_html/index.php`:**

   Скопируйте содержимое файла `public_html_index.php` из проекта в `~/public_html/index.php`
   
   **ВАЖНО:** Измените `olikonzp` на имя вашей директории проекта, если она называется по-другому:
   ```php
   $laravelPath = __DIR__ . '/../ваше_имя_директории';
   ```

3. **Скопируйте `.htaccess`:**
   ```bash
   cp ~/olikonzp/public/.htaccess ~/public_html/.htaccess
   ```

4. **Скопируйте `build/`:**
   ```bash
   cp -r ~/olikonzp/public/build ~/public_html/build
   ```

5. **Создайте симлинк для storage** (если поддерживается):
   ```bash
   cd ~/public_html
   ln -s ../olikonzp/storage/app/public storage
   ```
   
   Или если симлинки не работают, скопируйте:
   ```bash
   mkdir -p ~/public_html/storage
   cp -r ~/olikonzp/storage/app/public/* ~/public_html/storage/
   ```

6. **Настройте права:**
   ```bash
   chmod -R 775 ~/olikonzp/storage
   chmod -R 775 ~/olikonzp/bootstrap/cache
   chmod 755 ~/public_html
   chmod 644 ~/public_html/index.php
   chmod 644 ~/public_html/.htaccess
   ```

### ⚠️ Вариант 2: Все в public_html (менее безопасно, но проще)

Если нет доступа к уровню выше `public_html/`:

**Структура:**
```
public_html/
├── .htaccess          ← Из public/.htaccess
├── index.php          ← Из public/index.php (БЕЗ изменений)
├── app/
├── bootstrap/
├── config/
├── database/
├── resources/
├── routes/
├── storage/
├── vendor/
├── .env
└── build/             ← Из public/build/
```

**Шаги:**

1. Загрузите **ВСЕ** файлы Laravel проекта в `public_html/`
2. Скопируйте содержимое `public/` в корень `public_html/`
3. Удалите папку `public/` (она больше не нужна)
4. Настройте `.env` в `public_html/.env`

**Минусы:**
- Менее безопасно (файлы конфигурации доступны)
- Но работает, если нет другого выхода

## Проверка работы

После настройки:

1. Откройте сайт: `https://your-domain.com`
2. Должна открыться страница авторизации
3. Если видите ошибку - проверьте логи:
   ```bash
   tail -f ~/olikonzp/storage/logs/laravel.log
   ```

## Отладка ошибок

### 500 Internal Server Error

**Проверьте:**
1. Путь к Laravel в `public_html/index.php` правильный?
2. Существует ли папка `vendor/`?
3. Права доступа настроены?
4. Проверьте логи: `storage/logs/laravel.log`

**Решение:**
```bash
# Проверьте путь
cd ~/public_html
php -r "echo __DIR__ . '/../olikonzp';"  # Должен показать правильный путь

# Проверьте vendor/
ls -la ~/olikonzp/vendor/ | head -5

# Проверьте права
ls -la ~/olikonzp/storage/
```

### 403 Forbidden

**Причина:** Файлы Laravel находятся вне `public_html/` и не могут быть доступны.

**Решение:** Используйте Вариант 1 с правильным `index.php` в `public_html/`.

### Class '...' not found

**Причина:** `vendor/` не загружен или путь неправильный.

**Решение:**
```bash
# Проверьте наличие vendor/
ls -la ~/olikonzp/vendor/

# Если нет - загрузите с локальной машины
```

## Быстрая проверка структуры

```bash
# На сервере через SSH
cd ~
ls -la
# Должны быть: olikonzp/, public_html/

cd ~/public_html
ls -la
# Должны быть: .htaccess, index.php, build/

cd ~/olikonzp
ls -la
# Должны быть: app/, bootstrap/, config/, vendor/, storage/, .env
```

## Файлы для загрузки

1. **В `~/olikonzp/`:** Все файлы Laravel проекта (кроме `public/`)
2. **В `~/public_html/`:** 
   - `.htaccess` (из `public/.htaccess`)
   - `index.php` (используйте `public_html_index.php` с правильными путями)
   - `build/` (из `public/build/`)

## Автоматическая настройка (РЕКОМЕНДУЕТСЯ)

Используйте скрипт `setup_timeweb.sh`:

```bash
# 1. Загрузите скрипт на сервер в ~/olikonzp/
# 2. На сервере через SSH:
cd ~/olikonzp
chmod +x setup_timeweb.sh
./setup_timeweb.sh

# Или если проект называется по-другому:
./setup_timeweb.sh имя_вашего_проекта
```

Скрипт автоматически:
- ✅ Скопирует `.htaccess`
- ✅ Создаст правильный `index.php` в `public_html/`
- ✅ Скопирует `build/`
- ✅ Настроит права доступа
- ✅ Создаст симлинк для storage (если возможно)

## Ручная настройка

Если хотите настроить вручную:

```bash
# На сервере через SSH
cd ~

# 1. Скопируйте .htaccess
cp olikonzp/public/.htaccess public_html/.htaccess

# 2. Создайте index.php (используйте файл public_html_index.php из проекта)
# Загрузите public_html_index.php на сервер и скопируйте:
cp olikonzp/public_html_index.php public_html/index.php
# Или создайте вручную с правильными путями

# 3. Скопируйте build/
cp -r olikonzp/public/build public_html/build

# 4. Настройте права
chmod -R 775 olikonzp/storage olikonzp/bootstrap/cache
chmod 644 public_html/index.php public_html/.htaccess
```
